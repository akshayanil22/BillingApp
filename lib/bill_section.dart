import 'package:flutter/material.dart';

class BillSection extends StatelessWidget {
  const BillSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child:Text('Bill Section'));
  }
}
